{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
  
    {% for post in posts %}
        {# Utilisation de username car c'est une variable unique, onn est sur qu'il y en aura qu'une. #}
        
        <article>
             {# Utilisation de la method path pour faire un lien vers une autre page #}
            <a href="{{path('show', {id: post.id})}}" target="_blanck">
            <h2> {{post.title}} </h2></a>
            <p> {{post.summary | title}} </p>
            <time> {{post.publishedAt | date ('d M Y')| lower}} </time>
            {# <time> {{date(post.publishedAt).format('d M Y')}}</time> #}
        </article>
        <br>
        {% if post.author.username == ('jane_admin') %}
        <span>😃👱🏽‍♀️😃</span>
        {% endif %}
    {% endfor %}
    
</div>
{% endblock %}
