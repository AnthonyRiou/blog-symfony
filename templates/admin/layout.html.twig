{% extends 'base.html.twig' %}

{% block header_navigation_links %}

    <li class="nav-item {{ _route in ['admin_index', 'admin_post_index'] ? 'active' : ''}}">
		<a href="{{ path('admin_blog_index') }}" class="nav-link">
		    <i class="bi bi-list-columns" aria-hidden="true">Blog</i>
		</a>
	</li>
    <li class="nav-item">
	    <a href="{{ path('index') }}" class="nav-link">
	        <i class="bi bi-house-fill" aria-hidden="true">Admin</i>
		</a>
	</li>
    
{% endblock %}